local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RootUI = require(script.Parent.RootUI)
local Vide = require(ReplicatedStorage.Packages.Vide)
local mount = Vide.mount

return function(GUI) end
